Rank these movies by relevance to the search query.

Query: "{query}"

Movies:
{doc_list_str}

Return ONLY a valid JSON list of the movie IDs in order of relevance, best match first.
Do not include any explanation, markdown, or extra text. Example:

[2, 0, 4, 1, 3]
